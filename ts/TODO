var => let

<XXX> => as XXX

Er alle forekomster af string.format() blevet fjernet? (Svaret vil være nej fordi .format bruges ved
Answer.matchRegexp.)

Er alle copyright notices rettet?

Check erklæring af interfaces i techdoc

Der mangler en "Clear grammar"-knap ved øvelser

Tjek at alle strenge med `${yyy}` indeholder et $-tegn.
